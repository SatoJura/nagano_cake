<div class="container">
  <div class="row">
    <div class=" mx-auto">
      <!--エラー文-->
      <%= render "admin/items/error_msg", item: @item%>

      <%= form_with model:@item, url:admin_item_path, local:true do |f| %>
        <%= render :partial => 'item_form', :locals => {:f => f, :edit_flag => true} %>
      <% end%>
    </div>
  </div>
</div>